name: Dymos IPOPT-only debug

on:
  push:
    branches:
      - debug-dymos-model-equivalence-20260713

jobs:
  compare:
    name: ${{ matrix.problem }}
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        problem:
          - min_time_climb
    steps:
      - uses: actions/checkout@v7
      - uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}-${{ matrix.problem }}
          repository-cache: true
      - name: Run SIP with all configured regularization attempts
        continue-on-error: true
        run: >-
          bazel test
          //sip-optimal-control/casadi_problems/dymos:${{ matrix.problem }}
          //sip-qdldl/casadi_problems/dymos:${{ matrix.problem }}
          --cache_test_results=no
          --keep_going
          --test_env=SIP_CASADI_PROBLEMS_ABLATION=line_search_alpha_max_32_factor_attempts
          --test_env=SIP_CASADI_PROBLEMS_FACTOR_DIAGNOSTICS=1
          --test_output=all
