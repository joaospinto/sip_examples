load("//problem_definitions/casadi_problems/dymos:problem.bzl", "dymos_casadi_problem")
load("@sip_examples_pip//:requirements.bzl", "requirement")

dymos_casadi_problem(
    graph = True,
    extra_codegen_deps = [requirement("scipy")],
    num_factor_chunks = 32,
    num_product_chunks = 16,
    num_solve_chunks = 32,
)
