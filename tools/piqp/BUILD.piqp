load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "piqp",
    hdrs = glob([
        "include/piqp/**/*.hpp",
        "include/piqp/**/*.tpp",
    ]),
    includes = ["include"],
    deps = ["@eigen//:eigen"],
)
